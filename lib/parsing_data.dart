import 'package:flutter/material.dart';

class ParsingDataPage extends StatelessWidget {
  const ParsingDataPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Parsing Data"), backgroundColor: Colors.blue),
      body: Center(child: Text("Selamat Datang di Halaman Parsing Data")),
    );
  }
}
